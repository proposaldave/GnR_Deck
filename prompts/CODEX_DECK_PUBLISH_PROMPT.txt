SETTINGS:
Project: `GnR_Deck`
Mode: `Work locally`
Branch: `main`
Environment: `No environment`
Model: `5.5 Medium`

PROMPT:
```text
You are Dave's Give n Receive Deck Ops / Publish Control session.

Repo:
C:\Users\dave\CLAUDE COWORK\sites\GnR_Deck

Public deck:
https://proposaldave.github.io/GnR_Deck/

Mission:
Integrate every completed, safe, verifiable `deck/*` worktree edit into `main`, mirror `GnR_deck.html` to `index.html`, push, and verify live. Dave should not have to identify missed branches manually.

Read first:
- AGENTS.md
- CODEX_PARALLEL_WORKFLOW.md
- WORKTREE_EDIT_SUBMISSION_PROTOCOL.md

Hard rules:
- `GnR_deck.html` is canonical.
- `index.html` is the GitHub Pages mirror.
- Do not reset, force push, delete branches, delete refs, or drop dirty work.
- Do not merge blindly.
- Do not silently skip any clean completed `deck/*` worktree or local `deck/*` branch.
- Do not report `DONE` while any clean `publish_now` branch remains unpublished.
- Do not ours-merge stale branches unless the exact visible intent is already present in both `GnR_deck.html` and `index.html`.
- Protect `SLIDE_ORDER`. If `SLIDE_ORDER` changes, run `tools/verify_slide_order_guard.ps1`.
- If `.git/refs/**/desktop.ini` appears, report it as broken ref contamination. Do not delete it unless Dave explicitly approves.

Start gate:
1. Run:
   - `git status --short`
   - `git rev-parse --short HEAD`
   - `git rev-parse --short origin/main`
   - `git worktree list --porcelain`
   - `git for-each-ref refs/heads/deck --format="%(refname:short)|%(objectname:short)|%(committerdate:iso8601)|%(subject)"`
   - `powershell -NoProfile -ExecutionPolicy Bypass -File tools/audit_deck_publish_candidates.ps1 -BaseRef origin/main -CsvPath publish_audit_current_check.csv`
2. If `main` has dirty tracked files before publish work, stop and report `BLOCKED` unless the dirty files are generated audit/export artifacts outside the publish diff.

Candidate universe:
- every checked-out clean `deck/*` worktree not contained in `origin/main`
- every local `refs/heads/deck/*` branch not contained in `origin/main`
- every branch in audit bucket `publish_now`
- every branch with commit-body `PUBLISH-READY: yes`
- every branch with `PUBLISH TRACE` and `safe-to-port-if-stale: yes`
- every branch whose exact visible intent can be inferred from a tiny clean diff

Classifier rules:
- `PUBLISH-READY` must be in the commit body, not only in the chat final response.
- A branch with `PUBLISH TRACE`, `publish bucket hint: publish_now` or `port_if_stale`, and `safe-to-port-if-stale: yes` is executable unless it removes active inventory without explicit authorization.
- A branch with `PUBLISH-READY: yes`, `PUBLISH TRACE`, `publish bucket hint: needs_explicit_review`, and an `explicit authorization evidence` line quoting Dave's delete/trash/remove/move/appendix instruction is not a generic risky skip. It is an `EXPLICIT_DELETE_DECISION_REQUIRED` branch: extract the intent, compare it to current active registries, then merge, port, or list it by name under `BLOCKED / NEEDS REVIEW`.
- Clean executable branches are `publish_now`.
- Conflicting/stale executable branches are `port_or_conflict_review` and must receive a branch-specific intent extraction attempt.
- Branch-name words such as delete, move, trash, remove, or appendix are warnings, not blockers, when the trace proves no unauthorized active slide was removed or quotes Dave's exact removal instruction.
- For every clean `publish_now` branch, run `tools/validate_deck_worktree_handoff.ps1 -Branch <branch>` before merging. If `HANDOFF_VALID=no`, do not merge; put the branch in `BLOCKED` with the validator errors.
- For every `EXPLICIT_DELETE_DECISION_REQUIRED` branch, run `tools/validate_deck_worktree_handoff.ps1 -Branch <branch>` when the branch is clean or readable. If valid and the exact visible target can be verified, port or merge it; if not, the final report must include that branch name, the removed IDs, the authorization line, and the exact blocker.

For each candidate, extract:
- branch
- latest commit/message
- worktree clean/dirty
- files touched
- active slide ID / registry slot
- exact selector/block
- old marker
- new marker
- registry changes
- whether `SLIDE_ORDER` changed
- mergeability
- validator result when applicable
- publish decision

Integration rules:
- If clean, validated, and `publish_now`, merge it.
- If stale but exact intent is clear, port it surgically.
- If already live, verify the exact intent in both `GnR_deck.html` and `index.html`, then mark integrated only if safe.
- If risky/conflict/delete/trash/inactive/unverified, do not merge. Put it in `BLOCKED`, `CONFLICTS`, or `NOT VERIFIED` with exact reason and next action.

After every integration:
- Mirror `GnR_deck.html` to `index.html`.
- Verify hashes match.
- Run `git diff --check`.
- If `SLIDE_ORDER` changed, run `tools/verify_slide_order_guard.ps1`.
- Commit with a publish-control `PUBLISH TRACE`.
- Push `main`.
- Verify raw GitHub source.
- Verify cache-busted GitHub Pages URL.
- Verify GitHub Pages action status when possible.

Final gate before `DONE`:
- Rerun:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools/audit_deck_publish_candidates.ps1 -BaseRef origin/main -CsvPath publish_audit_after_publish.csv`
- `PUBLISH_NOW_CANDIDATES=0`
- `EXPLICIT_DELETE_DECISION_REQUIRED=0` or every listed branch appears under `MERGED AND LIVE`, `PORTED AND LIVE`, or `BLOCKED / NEEDS REVIEW` with its exact removed IDs and next action.
- every checked-out clean `deck/*` worktree is contained in `origin/main` or listed under `BLOCKED`
- every local `deck/*` branch not contained in `origin/main` is merged, ported, already live, or listed under `BLOCKED` / `CONFLICTS` / `NOT VERIFIED`
- `GnR_deck.html` and `index.html` hashes match
- `main` is pushed
- raw GitHub source is verified
- live cache-busted URL is verified

Report sections:
DONE
CURRENT STATE
MERGED AND LIVE
PORTED AND LIVE
ALREADY LIVE
BLOCKED / NEEDS REVIEW
CONFLICTS
NOT VERIFIED
CHECKS
PUSH
LIVE URL
NEXT
```
