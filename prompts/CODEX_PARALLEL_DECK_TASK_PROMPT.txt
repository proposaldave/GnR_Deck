You are editing the Give n Receive investor deck in a Codex Worktree.

MISSION
- Execute exactly one Dave deck edit request.
- Leave a local `deck/*` branch and commit that Publish Control can merge or port without Dave re-explaining the request.
- Codex owns verification. Dave should not have to find failed edits later.

MODE GATE
- This must be Worktree mode. If this is Local mode, stop and report `BLOCKED: wrong mode`.
- Start from current main-equivalent HEAD.
- Create a branch before editing: `deck/[short-task-name]`.
- End with a real commit.
- Do not push.

SOURCE GATE
- `GnR_deck.html` is the only default edit target.
- Do not edit `index.html` in a parallel worktree.
- Do not edit `pitch_visuals`.
- Do not regenerate images unless Dave explicitly asks.
- DECISION: Default to HTML/CSS/deck-code edits for `GnR_deck.html` because most deck work needs precision, active-slide verification, tiny diffs, and preservation of slide IDs, alts, nav, animation hooks, and brand typography.
- Do not refactor, reformat, lint, prettify, split files, or restructure the deck.
- Keep the diff tiny and localized.
- Preserve slide IDs, animation hooks, navigation, dot view, presenter behavior, and asset paths.
- Protect `SLIDE_ORDER`. Do not remove an active slide ID unless Dave explicitly requested that exact slide to be deleted, trashed, moved to appendix, or moved to an alt rail in this task.

IMAGE GENERATION VS DECK CODE
- Use deck code, not image generation, for copy edits, layout fixes, typography, spacing, labels, dots, arrows, icons, charts, diagrams, overlays, brand-color fixes, active/inactive alt wiring, screenshots, logos, video embeds, Drive iframe swaps, and anything where exact readable text is required.
- Use image generation only when the missing piece is a new text-free bitmap visual: hero/background image, cinematic or editorial community scene, metaphorical visual for reciprocity, human value, measurable connection, real-world community, or texture/section-breaker/atmospheric asset that HTML/CSS cannot produce cleanly.
- Generated images must be text-free, brand-compatible with warm cream / ink / gold / red, not generic SaaS/AI stock art, not blue-purple gradient tech art, not green, inserted only after active-slide/source path is identified, and paired with all readable text rendered separately in HTML.

ACCEPTANCE CONTRACT
Before patching, identify and use this as the pass/fail test:
- requested change
- annotation metadata if Dave supplied JSON: annotation slide ID, label/title, point coordinates when present, and exact note text
- expected active slide/section
- exact active selector/DOM line for each visible text or visual element
- old text/element that must be absent from that exact active selector after the edit
- new text/element that must be present in that exact active selector after the edit
- expected visible result
- files likely touched
- whether visual QA is required

ACTIVE SOURCE GATE
- If Dave supplied annotation JSON, use the annotation `slideId`, `slideLabel`, `slideTitle`, coordinates, and note text as target evidence.
- Annotation evidence does not replace active-source verification.
- Main-flow targets must be present in `SLIDE_ORDER`.
- Dot variants must be present in the correct `SLIDE_ALTS[slot]`.
- Staff targets must be present in `STAFF_ORDER`.
- Red dots are alternate candidates at the same slot, not separate deck slides.
- For visible text edits, patch the exact element that renders the visible text, not a nearby duplicate, eyebrow, comment, hidden block, backup block, appendix block, or inactive alt.
- If the target exists only in an inactive retained block or stale variant, do not edit it silently.
- If Dave's visible intent can be ported safely to the active slide, patch only the active slide and say so in `ACTIVE SOURCE`.
- If the active target cannot be identified, stop and report `BLOCKED` with the exact reason and next action.

EDIT GATE
- Edit only the active target unless Dave explicitly asks for variants.
- Do not patch inactive variants as a proxy for the visible deck.
- If changing `SLIDE_ORDER`, capture before/after active-slide IDs and report `ADDED_SLIDES` and `REMOVED_SLIDES`.
- Every removed active slide must map to Dave's explicit instruction.
- If a slide insertion/restoration shifts later slots, update affected `SLIDE_ALTS` keys in the same branch or report `BLOCKED`.

VALIDATION GATE
- Always run `git diff --check` before commit.
- Always verify the requested result exists in the active slide source and active registry path.
- Exact copy edits: verify old text is absent from the exact active selector/DOM line and new text is present in that same selector/DOM line.
- Do not pass source verification by finding new text elsewhere in the file.
- Visual/layout/image/order/animation/visibility edits require rendered visual QA of the active slide.
- If visual QA is required and cannot run, do not report `DONE`; report `BLOCKED` with the tooling failure and next action.
- If `SLIDE_ORDER` changed, run `tools/verify_slide_order_guard.ps1 -BaseRef HEAD -TargetRef WORKTREE` before commit. Unauthorized active-slide removal is a blocker.

SELF-CORRECTION LOOP
- If verification fails, attempt one targeted correction.
- Verify again.
- If verification still fails, report `BLOCKED` with root cause and next action.
- Do not keep guessing.

COMMIT GATE
- Commit subject: `deck: [short task name]`.
- The commit body must contain `PUBLISH-READY: yes`.
- The commit body must contain a `PUBLISH TRACE` block with:
  - request slug
  - annotation metadata if supplied: slide ID, label/title, point coordinates, and exact note text
  - publish bucket hint: `publish_now`, `port_if_stale`, or `needs_explicit_review`
  - safe-to-port-if-stale: yes/no, with reason
  - active slide ID or registry slot
  - exact active selector/DOM line for each changed visible element
  - old text absent from exact selector: yes/no
  - new text present in exact selector: yes/no
  - exact visible text markers changed
  - exact CSS selectors/properties changed for layout or positioning edits
  - exact registry keys changed for slide order, alts, trash, appendix, or staff order
  - `ADDED_SLIDES` and `REMOVED_SLIDES` if `SLIDE_ORDER` changed
  - explicit authorization evidence for every removed active slide ID, if any
  - exact asset paths added or changed
  - source verification command/result
  - visual QA method/result, or `visual QA: not required - exact source-only copy edit`
- If the requested result was already active and visible, create an empty commit with the same trace proving that result.
- Working tree must be clean after commit.

HANDOFF VALIDATOR
After commit, run:
`powershell -NoProfile -ExecutionPolicy Bypass -File tools/validate_deck_worktree_handoff.ps1 -Branch (git branch --show-current)`

- If it returns `HANDOFF_VALID=yes`, the branch can be reported publish-ready.
- If it returns `HANDOFF_VALID=no`, fix the branch/commit/trace/cleanliness issue or report `BLOCKED`.
- Do not report `DONE` when the validator fails.

PUBLISH BUCKET RULES
- Use `publish bucket hint: publish_now` for clean, verified, current edits that should merge directly.
- Use `publish bucket hint: port_if_stale` when the edit is tiny, mechanical, and safe to reapply if the branch becomes stale.
- Use `publish bucket hint: port_if_stale` for Dave-explicit delete/trash/appendix/move requests when the exact active slide ID or `SLIDE_ALTS[slot]` ID is named, the removal authorization is quoted, the registry diff is mechanical, and verification passes.
- Use `publish bucket hint: needs_explicit_review` only when the delete/trash/appendix/move target is ambiguous, removes active inventory without a quote of Dave's instruction, changes broad narrative structure, or cannot be verified.
- Use `safe-to-port-if-stale: yes` only when the exact selector/text/registry marker is named and mechanically verifiable.
- For explicit deletions, `safe-to-port-if-stale: yes` is allowed and preferred when the trace names the removed IDs, quotes Dave's deletion instruction, lists the registry keys changed, and `verify_slide_order_guard.ps1` passes for any `SLIDE_ORDER` change.
- Do not finish a branch as `needs_explicit_review` merely because the branch name includes delete/trash/remove. If Dave explicitly asked for that visible deletion and the active registry path is verified, make the branch executable.

HARD DONE RULE
Do not report `DONE` unless all are true:
- branch is `deck/[short-task-name]`
- commit exists
- working tree is clean
- requested edit is verified against the active source
- visual QA passed when required
- commit body contains `PUBLISH-READY: yes`
- commit body contains `PUBLISH TRACE`
- validator reports `HANDOFF_VALID=yes`
- nothing was pushed

FINAL RESPONSE RULES
- The `PUBLISH-READY` section must say either:
  - `PUBLISH-READY: yes - validator returned HANDOFF_VALID=yes`
  - `PUBLISH-READY: no - [exact blocker]`
- The final `PUBLISH TRACE` section must mirror the commit-body trace closely enough that Dave can see what will publish.
- The commit body remains the durable source of truth for Publish Control.

OUTPUT SECTIONS ONLY
DONE
BRANCH
COMMIT
PUBLISH-READY
TOUCHED
ACCEPTANCE CONTRACT
ACTIVE SOURCE
CHANGE
CHECKS
VISUAL QA
PUBLISH TRACE
VALIDATOR
PUSH
BLOCKED
