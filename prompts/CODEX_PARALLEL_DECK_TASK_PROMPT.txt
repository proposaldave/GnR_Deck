You are editing the Give n Receive investor deck in a Codex Worktree.

CORE PRINCIPLE
- Codex owns verification. Dave should not have to identify a failed edit after the fact.
- Do not report DONE unless the requested edit is proven in the active rendered deck path.

MODE CONTRACT
- This must be Worktree mode. If this is Local mode, stop and report BLOCKED.
- Start from current main-equivalent HEAD.
- Create a branch before editing: deck/[short-task-name]
- End with a real commit.
- Do not push.

SOURCE CONTRACT
- Edit only GnR_deck.html unless Dave explicitly requests assets.
- Do not edit index.html in a parallel edit session.
- Do not edit pitch_visuals.
- Do not regenerate images unless Dave explicitly asks.
- Do not refactor, reformat, lint, prettify, or restructure the deck.
- Keep the diff tiny and localized.
- Preserve slide IDs, animation hooks, navigation, dot view, presenter behavior, and asset paths.
- Protect SLIDE_ORDER as the active-slide inventory. Do not remove any existing ID from SLIDE_ORDER unless Dave explicitly requested that exact slide to be deleted, trashed, moved to appendix, or moved to an alt rail in this task.

ACCEPTANCE CONTRACT
- Before editing, parse Dave's request into:
  requested change
  expected active slide/section
  exact active selector/DOM line for each visible text or visual element
  old text/element that must be absent from that exact selector after the edit
  new text/element that must be present in that exact selector after the edit
  expected visible text/visual/layout result
  files likely touched
  whether visual QA is required
- Use this contract as the pass/fail test for the session.

ACTIVE SLIDE CONTRACT
- Before editing, identify the active source path.
- Main-flow targets must be present in SLIDE_ORDER.
- Dot variants must be present in the correct SLIDE_ALTS[slot].
- Staff targets must be present in STAFF_ORDER.
- Red dots are alternate candidates at the same slot, not separate deck slides.
- Confirm the requested target appears in the active slide source before patching.
- For visible text edits, identify the exact element that renders the visible text, not just a nearby duplicate, eyebrow, comment, alt, hidden block, or copied phrase elsewhere in the slide.
- If the target exists only in an inactive retained HTML block, backup block, deleted block, appendix-only block, or stale variant, do not edit it silently.
- If the user's intent can be ported safely to the active slide, patch only the active slide and say so in ACTIVE SOURCE.
- If the active target cannot be identified, stop and report BLOCKED with the exact reason and next step.

EDIT CONTRACT
- Edit only the active target unless Dave explicitly asks for variants.
- Do not patch inactive variants as a proxy for the visible deck.
- Keep the diff tiny.
- Preserve navigation, animation hooks, slide IDs, and the design system.
- If changing SLIDE_ORDER, capture before/after active-slide IDs. Report ADDED_SLIDES and REMOVED_SLIDES. Every removed slide must map to Dave's explicit request.
- If an active slide ID disappears accidentally while merging, moving, restoring, or editing another slide, restore it before commit.
- If a slide insertion or restore shifts later slots, update affected SLIDE_ALTS keys in the same branch or report BLOCKED.

VALIDATION
- Source verification is required for every edit: requested result exists in the active slide source and the active slide ID/registry path is correct.
- For exact copy edits: verify old text absent from the exact active selector/DOM line, new text present in that exact active selector/DOM line, and active slide source contains the requested result.
- Do not pass source verification by finding the new text elsewhere in the same slide, an eyebrow, alt, comment, backup block, appendix block, or inactive duplicate.
- For visual/layout/copy-positioning/image/order changes: run visual/render verification and verify the rendered active slide shows the requested outcome.
- If SLIDE_ORDER changed, run `tools/verify_slide_order_guard.ps1 -BaseRef HEAD -TargetRef WORKTREE` before commit. If it reports unauthorized removed slides, restore them or report BLOCKED. Do not commit a branch that accidentally removes an active slide.
- If visual QA cannot run, do not report DONE for visual edits; report BLOCKED or ask Dave to approve source-only.
- If browser/Playwright QA fails for tooling reasons, report the limitation and do source verification instead.
- Run git diff --check before commit.

SELF-CORRECTION LOOP
- If verification fails, attempt one targeted correction.
- Verify again.
- If verification still fails, report BLOCKED with root cause and next step.
- Do not keep guessing.

COMMIT
- Commit message subject: deck: [short task name]
- Commit message body must include a publish-readable `PUBLISH TRACE` block with:
  request slug
  publish bucket hint: `publish_now`, `port_if_stale`, or `needs_explicit_review`
  safe-to-port-if-stale: yes/no, with reason
  active slide ID or registry slot
  exact active selector/DOM line for each changed visible element
  old text absent from exact selector: yes/no
  new text present in exact selector: yes/no
  exact visible text markers changed
  exact CSS selectors/properties changed for layout or positioning edits
  exact registry keys changed for slide order, alts, trash, appendix, or staff order
  ADDED_SLIDES and REMOVED_SLIDES if SLIDE_ORDER changed
  explicit authorization evidence for every removed active slide ID, if any
  exact asset paths added or changed
  source verification command/result
  visual QA method/result when required
- If the edit is a no-op because the requested result was already active and visible, still create an empty commit with a PUBLISH TRACE proving that result.
- Working tree must be clean after commit.

PUBLISH TRACE
- The final response must leave publish-readable evidence so Publish Control can verify and publish the branch without Dave re-explaining the request.
- The commit body must also leave publish-readable evidence so Publish Control can recover the intent even if the chat final response is not available.
- Include:
  request slug
  publish bucket hint: `publish_now`, `port_if_stale`, or `needs_explicit_review`
  safe-to-port-if-stale: yes/no, with reason
  active slide ID or registry slot
  exact active selector/DOM line for each changed visible element
  old text absent from exact selector: yes/no
  new text present in exact selector: yes/no
  exact visible text markers changed
  exact CSS selectors/properties changed for layout or positioning edits
  exact registry keys changed for slide order, alts, trash, appendix, or staff order
  ADDED_SLIDES and REMOVED_SLIDES if SLIDE_ORDER changed
  explicit authorization evidence for every removed active slide ID, if any
  exact asset paths added or changed
  source verification command/result
  visual QA method/result when required
- For visual/layout/copy-positioning edits, do not say only "visual QA passed." Name the active slide ID and the selector/property or rendered element position that passed.
- If the edit request changes two visible elements, list both elements in the acceptance contract and verification note. Do not report DONE after verifying only one of them.
- If the requested old text still appears in the same active selector/DOM line after editing, report BLOCKED instead of DONE, even if the replacement appears elsewhere in the file.
- If the edit request deletes, trashes, moves to appendix, or removes a slide/alt/staff item, quote Dave's exact removal instruction in the PUBLISH TRACE. Without that evidence, Publish Control must block the branch.
- If the edit is small and mechanical, explicitly set `safe-to-port-if-stale: yes` and name the exact text/selector/registry marker Publish Control should verify after porting.
- Make the branch name and commit message specific enough that Publish Control can match the branch to Dave's request.

HARD DONE RULE
- Do not report DONE unless:
  work is on a deck/[short-task-name] branch
  a commit exists
  working tree is clean
  requested edit is verified against the active slide source
  visual/render verification passed when required
  commit body contains PUBLISH TRACE
  nothing was pushed

OUTPUT SECTIONS ONLY
DONE
BRANCH
COMMIT
TOUCHED
ACCEPTANCE CONTRACT
ACTIVE SOURCE
CHANGE
CHECKS
VISUAL QA
PUBLISH TRACE
VERIFICATION NOTE
PUSH
BLOCKED

End with:
My edit request:
[PASTE EXACT REQUEST HERE]
